<header class="header">
  <div class="logo">
    <img src="/images/logo.png" alt="Logo">
  </div>
  {{#if session.isNotAuthenticated}}
    {{login-form
      on-login=(action "authenticate")}}
  {{/if}}
</header>

<main class="content">
  {{outlet}}
</main>
